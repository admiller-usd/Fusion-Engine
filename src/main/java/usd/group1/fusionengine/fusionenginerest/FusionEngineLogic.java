package usd.group1.fusionengine.fusionenginerest;

import org.apache.logging.log4j.LogManager;
import org.apache.logging.log4j.Logger;

/**
 * The class that handles the logic for storing
 * coordinates and returning different formats
 */
public class FusionEngineLogic {

    /* The constant logger */
    private static final Logger logger = LogManager.getLogger(FusionEngineLogic.class);

    /* Needs to take in two coordinates and should
       generate a "unique" ID so someone can query
       for the results later on */


}
